/// @description Draw the spell circle
event_inherited();

#region Body
for(var i = 0; i < 7; i++) {
	var slot = param_slots[i];
	
	var param = slot[? "In"];
	
	if(param != -1) {
		if(ds_exists(param, ds_type_map)) {
			
		}
	}
}
#endregion